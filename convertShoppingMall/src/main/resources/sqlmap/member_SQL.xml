<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE  mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="member">

	<insert id="create" parameterType="hashMap">
		insert into
		member(id,passwd,name,regist_day)
		values(#{id},#{passwd},#{name},now()
		)
	</insert>

	<select id="select_detail" parameterType="hashMap"
		resultType="hashMap">
		select * from member where id = #{id}
	</select>
	<update id="update" parameterType="hashMap">
		update member
		set name = #{name}, regist_day = now()
		where id = #{id}
	</update>

	<!-- <select id="list" parameterType="hashMap" resultType="hashMap"> -->
	<select id="list" parameterType="string" resultType="MemberVO">	
		select *
		from member
		<if test="search != null and search != ''">
			where
			name like concat('%',trim(#{search}),'%') or id like
			concat('%',trim(#{search}),'%')
		</if>
	</select>

	<delete id="delete" parameterType="hashMap">
		delete from member where id =
		#{id}
	</delete>

	<!-- <select id="search" parameterType="hashMap" resultType="hashMap"> select 
		* from member <if test="search != null and search != ''"> where name like 
		concat('%',#{search},'%') or id like concat('%',#{search},'%') </if> </select> -->



</mapper>  